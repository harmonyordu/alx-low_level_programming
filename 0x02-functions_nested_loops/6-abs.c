#include "main.h"

/**
 *_abs - The main function of the program
 *@n: The variable to make absolute
 *Return: int or int times a negative number
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}

	else
		return (n * -1);

}
